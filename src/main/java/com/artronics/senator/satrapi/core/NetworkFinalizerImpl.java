package com.artronics.senator.satrapi.core;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Component;

@Component
public class NetworkFinalizerImpl implements NetworkFinalizer
{
    private final static Logger log = Logger.getLogger(NetworkFinalizerImpl.class);

}
